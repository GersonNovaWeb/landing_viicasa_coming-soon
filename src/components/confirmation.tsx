'use client';
import {useLanguage} from '@/components/language';

import{useState}from'react';import Link from'next/link';import{api}from'@/lib/client';
export function Confirmation(){const {t} = useLanguage();const[busy,setBusy]=useState(false),[done,setDone]=useState(false),[error,setError]=useState('');return <section className="account-card"><p className="eyebrow">{t("UN ÚLTIMO PASO")}</p><h1>{done?t('Bienvenido a VIICASA.'):t('Confirma tu correo.')}</h1><p>{done?t('Tu registro fue confirmado. Gracias por acompañarnos desde el comienzo.'):t('Pulsa el botón para confirmar que solicitaste información de VIICASA.')}</p>{!done&&<button className="button" disabled={busy} onClick={async()=>{setBusy(true);setError('');try{const token=window.location.hash.slice(1);await api('confirm',{token});window.history.replaceState(null,'',window.location.pathname);setDone(true);}catch(e){setError(e instanceof Error?e.message:t('No se pudo confirmar.'));}finally{setBusy(false);}}}>{busy?t('Confirmando…'):t('Confirmar mi correo')}</button>}{error&&<p className="error-message" role="alert">{t(error)}</p>}<Link href="/" className="text-link">{t("Volver a VIICASA")}</Link></section>}
