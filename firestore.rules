rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Browsers never access personal data directly; Node.js authenticates and authorizes each request.
    match /{document=**} { allow read, write: if false; }
  }
}
